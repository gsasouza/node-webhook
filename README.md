# Node webhook manager

## Features
 - use rabbitMQ server to manage queues
 - queue with messages, separated by tries on delivery
 - logger with winston and sentry
 - producer and consumer that can run independetly
